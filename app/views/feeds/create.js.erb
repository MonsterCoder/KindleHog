$("top_row_content").update("");  
$("entriesCount").update("<h1><%=escape_javascript(@entries.count.to_s)%> new items added</h1>");  
$("feed_entry").update("<%= escape_javascript(render :partial=> "entry", :collection => @entries)%>");  
